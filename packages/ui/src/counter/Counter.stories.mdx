<!-- Counter.stories.mdx -->

import { Canvas, Meta, Story, Props, ArgsTable } from "@storybook/addon-docs";
import { Counter } from "./Counter";

import CounterExampleSource from "!raw-loader!./examples/Counter.example.tsx";
import { CounterExample } from "./examples/Counter.example.tsx";
import { Import } from "../utils/doc";

<Meta
  title="Counter"
  component={Counter}
  argTypes={{
    count: {
      description: "Счетчик",
      control: "number",
      defaultValue: "0",
    },
    size: {
      description: "Размер элемента",
      options: ["small", "default", "undefined"],
      control: { type: "radio" },
      table: {
        category: "Размер",
      },
    },
  }}
/>

<Import
  module={["Counter"]}
  path={"@ya.praktikum/react-developer-burger-ui-components"}
/>

export const Template = (args) => <Counter {...args} />;

# Counter

Элемент `Counter` используется для установки счетчика выбранных ингредиентов

<Canvas mdxSource={CounterExampleSource}>
  <Story name="Counter">{CounterExample.bind({})}</Story>
</Canvas>

<ArgsTable story="Counter" />
