<!-- Button.stories.mdx -->

import { Canvas, Meta, Story, Props, ArgsTable } from "@storybook/addon-docs";

import { ButtonExample } from "./examples/Button.example";

import ButtonExampleSource from "!raw-loader!./examples/Button.example";

import { Button } from "./Button";
import { Import } from "../utils/doc";

<Meta
  title="Button"
  component={Button}
  argTypes={{
    value: {
      description: "Значение кнопки",
      control: "text",
      defaultValue: "Кнопка",
    },
  }}
/>

<Import
  module={["Button"]}
  path={"@ya.praktikum/react-developer-burger-ui-components"}
/>

export const Template = (args) => <Button {...args} />;

<Canvas>
  <Story name="argTypes">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="argTypes" />

# Пример использования кнопки

<Canvas mdxSource={ButtonExampleSource}>
  <Story name="Button">{ButtonExample.bind({})}</Story>
</Canvas>
