<!-- Input.stories.mdx -->

import { Canvas, Meta, Story, Props, ArgsTable } from "@storybook/addon-docs";

import { Input } from "./Input";

import InputExampleSource from "!raw-loader!./examples/Input.example.tsx";
import { InputExample } from "./examples/Input.example.tsx";
import { Import } from "../utils/doc";

<Meta
  title="Input"
  component={Input}
  argTypes={{
    type: {
      description: "Тип инпута",
      options: ["text", "email", "password"],
      control: { type: "radio" },
      table: {
        category: "Тип",
      },
      defaultValue: "text"
    },
    value: {
      description: "Значения email адреса",
      control: "text",
      defaultValue: "example@mail.ru",
    },
    name: {
      description: "Название элемента",
      control: "text",
      defaultValue: "Название элемента",
    },
    placeholder: {
      description: "Название поля",
      control: "text",
      defaultValue: "Название поля",
    },
    success: {
      description: "Корректное заполнение поля",
      control: "boolean",
    },
    error: {
      description: "Некорректное заполнение поля",
      control: "boolean",
    },
    disabled: {
      description: "Отключение поля",
      control: "boolean",
    },
    errorText: {
      description: "Текст, который выводится при ошибке",
      control: "text",
      defaultValue: "Произошла ошибка",
    },
    size: {
      description: "Размер элемента",
      options: ["small", "default"],
      control: { type: "radio" },
      table: {
        category: "Размер",
      },
      defaultValue: "default",
    },
    onChange: {
      description: "Обработчик изменения значения элемента",
      table: {
        category: "Events",
      },
    },
    onIconClick: {
      description: "Обработчик клика на элемент",
      table: {
        category: "Events",
      },
    },
    onBlur: {
      description: "Обработчик потери элементом фокуса",
      table: {
        category: "Events",
      },      
    },
    onFocus: {
      description: "Обработчик события при получении элементом фокуса",
      table: {
        category: "Events",
      },
    },
    ref: {
      table: { disable: true },
    },
    key: {
      table: { disable: true },
    },
  }}
/>

<Import
  module={["Input"]}
  path={"@ya.praktikum/react-developer-burger-ui-components"}
/>

export const Template = (args) => <Input {...args} />;

# Input

Элемент `Input` используется в качестве полей ввода емейла, 

<Canvas>
  <Story name="argTypes">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="argTypes" />

# Пример использования

Корректный пример кода для использования ui-элемента

<Canvas mdxSource={InputExampleSource}>
  <Story name="useCase">
    {InputExample.bind({})}
  </Story>
</Canvas>

