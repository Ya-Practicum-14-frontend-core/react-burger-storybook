<!-- EmailInput.stories.mdx -->

import { Canvas, Meta, Story, Props, ArgsTable } from "@storybook/addon-docs";
import { EmailInput } from "./EmailInput";
import EmailInputExampleSource from "!raw-loader!./examples/EmailInput.example.tsx";
import { EmailInputExample } from "./examples/EmailInput.example.tsx";
import { Import } from "../utils/doc";

<Meta
  title="EmailInput"
  component={EmailInput}
  argTypes={{
    value: {
      description: "Значения email адреса",
      control: "text",
      defaultValue: "example@mail.ru",
    },
    name: {
      description: "Название элемента",
      control: "text",
      defaultValue: "",
    },
    size: {
      description: "Размер элемента",
      options: ["small", "default"],
      control: { type: "radio" },
      table: {
        category: "Размер",
      },
      defaultValue: "default",
    },
    onChange: {
      description: "Обработчик изменения значения элемента",
      table: {
        category: "Events",
      },
    },
  }}
/>

<Import
  module={["EmailInput"]}
  path={"@ya.praktikum/react-developer-burger-ui-components"}
/>

export const Template = (args) => <EmailInput {...args} />;

# EmailInput

Элемент `EmailInput` используется в построение компонента для внесения значения email адреса интерфейса `Profile`

<Canvas>
  <Story name="argTypes">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="argTypes" />

# Пример использования

Корректный пример кода для использования ui-элемента

<Canvas mdxSource={EmailInputExampleSource}>
  <Story name="useCase">{EmailInputExample.bind({})}</Story>
</Canvas>
