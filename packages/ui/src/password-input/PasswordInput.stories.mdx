<!-- PasswordInput.stories.mdx -->

import { Canvas, Meta, Story, Props, ArgsTable } from "@storybook/addon-docs";

import { PasswordInput } from "./PasswordInput";

import PasswordInputExample from "!raw-loader!./examples/PasswordInput.example.tsx";

<Meta
  title="PasswordInput"
  component={PasswordInput}
  argTypes={{
    value: {
      description: "Пароль",
      control: "text",
      defaultValue: "pa55w0rd",
    },
    name: {
      description: "Имя элемента",
      control: "text",
      defaultValue: "password",
    },
    size: {
      description: "Размер элемента",
      options: ["small", "default", "undefinded"],
      control: { type: "radio" },
      table: {
        category: "Параметры отображения",
      },
    },
    onChange: {
      description: "Обработчик изменения значения элемента",
      table: {
        category: "Events",
      },
    },
  }}
/>

export const Template = (args) => <PasswordInput {...args} />;

# PasswordInput

Элемент `PasswordInput` используется при вводе пароля

<Canvas>
  <Story name="argTypes">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="argTypes" />

# Пример использования

Корректный пример кода для использования ui-элемента

<Canvas mdxSource={PasswordInputExample}>
  <Story name="useCase">{Template.bind({})}</Story>
</Canvas>
