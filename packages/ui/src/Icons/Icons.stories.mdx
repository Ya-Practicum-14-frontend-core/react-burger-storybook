<!-- Icons.stories.mdx -->

import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";

import { IconsExample } from "./examples/Icons.example";
import IconsExampleSource from "!raw-loader!./examples/Icons.example";
import { Import } from "../utils/doc";

import {
  IconsList,
  CurrencyIcon,
  BurgerIcon,
  LockIcon,
  DragIcon,
  CloseIcon,
  CheckMarkIcon,
  ListIcon,
  ProfileIcon,
  EditIcon,
  InfoIcon,
  ShowIcon,
  HideIcon,
  LogoutIcon,
  DeleteIcon,
  ArrowUpIcon,
  ArrowDownIcon,
  MenuIcon,
} from "./Icons";

<Meta
  title="Icons"
  component={CurrencyIcon}
  parameters={{
    backgrounds: {
      default: "dark",
      values: [
        { name: "dark", value: "#1c1c21" },
        { name: "light", value: "#f5f5f5" },
      ],
    },
  }}
  argTypes={{
    type: {
      description: "Определяет цвет иконок",
      options: ["primary", "secondary", "error", "success"],
      control: { type: "radio" },
      defaultValue: "success",
      table: {
        category: "Внешний вид",
      },
    },
    onClick: {
      description: "Обработчик нажатия на иконку",
      table: {
        category: "Событие",
      },
    },
  }}
/>

<Import
  module={["CurrencyIcon"]}
  path={"@ya.praktikum/react-developer-burger-ui-components"}
/>

export const Template = (args) => <IconsExample {...args} />;

# Icons

Элементы `Icons` используются в построении интерфейса

<Canvas mdxSource={IconsExampleSource}>
  <Story name="Icons">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Icons" />
