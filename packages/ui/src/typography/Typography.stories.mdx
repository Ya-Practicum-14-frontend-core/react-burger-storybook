<!-- Typography.stories.mdx -->

import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import { Typography } from "./Typography";

import { TypographyExample } from "./examples/Typography.example.tsx";
import TypographyExampleSource from "!raw-loader!./examples/Typography.example.tsx";

<Meta
  title="Typography"
  component={Typography}
  argTypes={{
    value: {
      description: "Текст",
      control: "text",
      defaultValue: "Пример",
    },
    color: {
      description: "Цвет текста",
      control: { type: "radio" },
      defaultValue: "text_color_success",
    },
    type: {
      description: "Цвет текста",
      control: { type: "radio" },
      defaultValue: "text_type_main-default",
    },
  }}
/>

export const Template = (args) => <Typography {...args} />;

# Typography

Элемент `Typography`

<Canvas mdxSource={TypographyExampleSource}>
  <Story name="Typography">{Template.bind({})}</Story>
</Canvas>

<ArgsTable of={Typography} />
